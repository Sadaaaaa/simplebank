apiVersion: v1
kind: Secret
metadata:
  name: {{ include "accounts.fullname" . }}-db-secret
  labels:
    {{- include "accounts.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.config.database.password | b64enc }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "accounts.fullname" . }}-oauth-secret
  labels:
    {{- include "accounts.labels" . | nindent 4 }}
type: Opaque
data:
  client-secret: {{ .Values.config.auth.server.client.secret | b64enc }} 