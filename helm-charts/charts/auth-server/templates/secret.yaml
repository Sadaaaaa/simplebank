apiVersion: v1
kind: Secret
metadata:
  name: {{ include "auth-server.fullname" . }}-db-secret
  labels:
    {{- include "auth-server.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.config.database.password | b64enc }} 